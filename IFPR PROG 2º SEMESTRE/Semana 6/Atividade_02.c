#include <stdlib.h>
#include <string>

int main(){
    int i, count = 0, teste = 0;
    char palavra[20];
    printf("Digite duas palavras: ");
    char *p1[20], *p2[20];

    for(i = 0; i < 2; i++){
        for(int x = 0; x != '\0'; x++){
            teste = strcmp(p1[x], p2[x]);
            if(teste )
        }
    }
}